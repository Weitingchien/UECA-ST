# 為什麼 nonzero(as_tuple=False) 的結果形狀是 (3, 1)？

## 問題

```python
result = bool_tensor.nonzero(as_tuple=False)
# 返回: [[6], [7], [8]]
# 形狀: (3, 1)

# 為什麼是 (3, 1)？
```

---

## 核心答案

### `.nonzero()` 的設計邏輯

`.nonzero()` 方法的設計目的是**找出所有非零元素的多維座標（坐標）**

```
對於一維張量：坐標 = 單一位置
對於二維張量：坐標 = (行, 列) 對
對於三維張量：坐標 = (深度, 行, 列) 對
對於 n 維張量：坐標 = (dim0, dim1, ..., dimN-1) 對
```

---

## 一維張量的例子

### 輸入：一維布林張量

```python
import torch

# 一維張量
bool_tensor = torch.tensor([False, False, True, True, False, True])
# 位置:           [0]      [1]    [2]   [3]   [4]   [5]
# 形狀: (6,)

# 非零元素的位置：
# - 位置 2 有 True
# - 位置 3 有 True
# - 位置 5 有 True
```

### 輸出：nonzero 的結果

```python
result = bool_tensor.nonzero(as_tuple=False)

print(result)
# 輸出:
# tensor([[2],
#         [3],
#         [5]])

print(result.shape)
# 輸出: torch.Size([3, 1])
```

### 形狀分析：為什麼是 `(3, 1)`？

```
二維結構：
┌──────────────┐
│   第 0 列    │
│  坐標編號    │
├──────────────┤
│     [2]      │  ← 第 0 行：第 1 個非零位置的坐標
│     [3]      │  ← 第 1 行：第 2 個非零位置的坐標
│     [5]      │  ← 第 2 行：第 3 個非零位置的坐標
└──────────────┘

行數（rows）= 3  ← 有 3 個非零元素
列數（cols）= 1  ← 每個坐標有 1 個數字（因為是一維張量）

形狀 = (3, 1)
```

### 邏輯解釋

```
一維張量的坐標只需要**一個數字**來表示位置
所以每一行只有 1 列

結果格式：
[[位置1],
 [位置2],
 [位置3]]
```

---

## 二維張量的例子（理解為什麼一維是 1 列）

### 輸入：二維布林張量

```python
import torch

# 二維張量
bool_tensor_2d = torch.tensor([[False, True, False],
                                [True, False, False],
                                [False, False, True]])
# 形狀: (3, 3)

# 非零元素位置（用 (行, 列) 表示）：
# - (0, 1) ← 第 0 行，第 1 列
# - (1, 0) ← 第 1 行，第 0 列
# - (2, 2) ← 第 2 行，第 2 列
```

### 輸出：nonzero 的結果

```python
result = bool_tensor_2d.nonzero(as_tuple=False)

print(result)
# 輸出:
# tensor([[0, 1],
#         [1, 0],
#         [2, 2]])

print(result.shape)
# 輸出: torch.Size([3, 2])  ← 注意：是 2 列！
```

### 形狀分析：為什麼是 `(3, 2)`？

```
二維結構：
┌───────────────────────┐
│ 第 0 列 │ 第 1 列    │
│  (行)   │  (列)      │
├───────────────────────┤
│   0    │    1      │  ← 第 0 行：位置 (0, 1)
│   1    │    0      │  ← 第 1 行：位置 (1, 0)
│   2    │    2      │  ← 第 2 行：位置 (2, 2)
└───────────────────────┘

行數（rows）= 3  ← 有 3 個非零元素
列數（cols）= 2  ← 每個坐標有 2 個數字（因為是二維張量）

形狀 = (3, 2)
```

### 規律發現

```
張量維度 → 坐標需要的數字個數 → nonzero 結果的列數

一維張量 (6,)     → 1 個數字  → 結果列數 = 1  → (n, 1)
二維張量 (3, 3)   → 2 個數字  → 結果列數 = 2  → (n, 2)
三維張量 (2, 3, 4) → 3 個數字 → 結果列數 = 3  → (n, 3)
```

---

## 回到原問題：一維張量 → (3, 1)

### 重新理解 UECA 中的例子

```python
# 輸入：mask_ids（一維張量）
mask_ids = torch.tensor([101, 122, 127, 3299, 127, 3189, 103, 103, 103, 102, ...])
#           形狀: (512,)  ← 一維

# 操作：找非零位置（即找值為 103 的位置）
bool_tensor = (mask_ids == 103)
# 結果: [False, False, ..., True, True, True, ...]
# 形狀: (512,)  ← 仍然是一維

# nonzero 操作
result = bool_tensor.nonzero(as_tuple=False)
# 輸出: tensor([[6], [7], [8], [22], [23], [24], ...])
# 形狀: (n, 1)
```

### 為什麼結果是 `(n, 1)`？

```
原始張量的維度 = 1（一維）
    ↓
每個非零元素的坐標需要 1 個數字
    ↓
nonzero 結果有 1 列
    ↓
形狀 = (3, 1)
```

---

## 完整邏輯流程圖

```
輸入 mask_ids
│
├─ 形狀: (512,)  ← 一維張量
│
└─ 進行比較 == 103
   │
   ├─ 結果: [False, False, ..., True, True, True, ...]
   │
   └─ 形狀: (512,)  ← 仍然是一維
      │
      └─ 套用 .nonzero(as_tuple=False)
         │
         ├─ 目的：找非零位置的坐標
         │
         ├─ 一維張量的坐標 = 單一位置編號
         │  (需要 1 個數字表示)
         │
         ├─ 找到 3 個非零位置：[6], [7], [8]
         │
         └─ 返回結果：
            tensor([[6],
                    [7],
                    [8]])
            
            ↓
            形狀 = (3, 1)
            ↓
            3 個非零位置 × 1 個坐標數字
```

---

## 對比理解

### 一維張量的坐標系

```
位置：  0    1    2    3    4    5
值：   [F]  [F]  [T]  [T]  [F]  [T]

坐標表示：
- 位置 2 → 坐標 [2]      ← 只需 1 個數字
- 位置 3 → 坐標 [3]      ← 只需 1 個數字
- 位置 5 → 坐標 [5]      ← 只需 1 個數字

nonzero 結果：
[[2],
 [3],
 [5]]

形狀：(3, 1)  ← 3 個坐標，每個坐標 1 個數字
```

### 二維張量的坐標系

```
    0   1   2
0 [ F ] T [ F ]
1 [ T ] F [ F ]
2 [ F ] F [ T ]

坐標表示：
- 位置 (0,1) → 坐標 [0, 1]   ← 需要 2 個數字
- 位置 (1,0) → 坐標 [1, 0]   ← 需要 2 個數字
- 位置 (2,2) → 坐標 [2, 2]   ← 需要 2 個數字

nonzero 結果：
[[0, 1],
 [1, 0],
 [2, 2]]

形狀：(3, 2)  ← 3 個坐標，每個坐標 2 個數字
```

---

## 公式總結

```
nonzero(as_tuple=False) 結果的形狀計算：

行數 = 非零元素的個數
列數 = 原始張量的維度（維度數）

例子：
一維張量 (512,)：
  ├─ 維度 = 1
  └─ 結果列數 = 1 → (非零個數, 1)

二維張量 (3, 3)：
  ├─ 維度 = 2
  └─ 結果列數 = 2 → (非零個數, 2)

三維張量 (2, 3, 4)：
  ├─ 維度 = 3
  └─ 結果列數 = 3 → (非零個數, 3)
```

---

## 實際代碼驗證

### 一維 → (n, 1)

```python
import torch

# 一維
t1 = torch.tensor([False, False, True, True, False, True])
r1 = t1.nonzero(as_tuple=False)
print(f"一維 {t1.shape} → {r1.shape}")
# 輸出: 一維 torch.Size([6]) → torch.Size([3, 1])
#       ↓形狀變化
#       (6,) → (3, 1)
```

### 二維 → (n, 2)

```python
# 二維
t2 = torch.tensor([[False, True, False],
                   [True, False, False],
                   [False, False, True]])
r2 = t2.nonzero(as_tuple=False)
print(f"二維 {t2.shape} → {r2.shape}")
# 輸出: 二維 torch.Size([3, 3]) → torch.Size([3, 2])
#       ↓形狀變化
#       (3, 3) → (3, 2)
```

### 三維 → (n, 3)

```python
# 三維
t3 = torch.ones(2, 3, 4, dtype=torch.bool)
t3[0, 1, 2] = False
t3[1, 2, 3] = False
r3 = t3.nonzero(as_tuple=False)
print(f"三維 {t3.shape} → {r3.shape}")
# 輸出: 三維 torch.Size([2, 3, 4]) → torch.Size([22, 3])
#       ↓形狀變化
#       (2, 3, 4) 有 22 個 True → (22, 3)
```

---

## 重點總結

| 項目 | 說明 |
|------|------|
| **原始張量形狀** | (512,)  ← 一維 |
| **非零個數** | 3 個（位置 6, 7, 8） |
| **每個坐標的維度** | 1（一維張量坐標只需 1 個數字） |
| **結果形狀** | (3, 1) = (非零個數, 坐標維度) |
| **為什麼是 1 列？** | **因為原始張量是一維的** |
| **為什麼是 3 行？** | **因為有 3 個非零元素** |

---

## 類比理解

### 座標系類比

```
一維座標系（像數軸）：
    0   1   2   3   4   5
    ┼───┼───┼───┼───┼───┼
    [F] [F] [T] [T] [F] [T]
                ↑   ↑       ↑
          位置 2  位置 3  位置 5

每個位置的坐標 = 1 個數字
nonzero 結果 = [[2], [3], [5]]
形狀 = (3, 1)


二維座標系（像棋盤）：
    0   1   2
0 [F] [T] [F]
1 [T] [F] [F]
2 [F] [F] [T]

每個位置的坐標 = 2 個數字（行, 列）
nonzero 結果 = [[0,1], [1,0], [2,2]]
形狀 = (3, 2)
```

---

## NumPy 對比

### NumPy 方式

```python
import numpy as np

arr = np.array([False, False, True, True, False, True])

# NumPy 的 nonzero
result = np.nonzero(arr)
print(result)
# 輸出: (array([2, 3, 5]),)  ← 元組，內含一維陣列

print(result[0])
# 輸出: [2 3 5]  ← 一維

# NumPy 沒有 as_tuple 參數，總是返回元組
# 但結果本質上是一維的
```

### PyTorch vs NumPy 對比

| 方式 | 返回格式 | 形狀 | 含義 |
|------|---------|------|------|
| PyTorch `as_tuple=False` | 張量 | (3, 1) | **二維張量** |
| PyTorch `as_tuple=True` | 元組 | (3,) | **元組內含一維張量** |
| NumPy | 元組 | (3,) | **元組內含一維陣列** |

